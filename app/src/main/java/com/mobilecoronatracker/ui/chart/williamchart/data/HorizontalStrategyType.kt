package com.mobilecoronatracker.ui.chart.williamchart.data

enum class HorizontalStrategyType {
    DEFAULT, // Just squeeze all into text labels area
    PICK_AND_MATCH // Pick as many labels as possible without obscuring the others;
}